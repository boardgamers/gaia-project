{
  "license": "MIT",
  "scripts": {
    "commit-msg-check": "node scripts/check-commit-msg.js",
    "lint-staged": "lint-staged",
    "postinstall": "husky install",
    "prettier": "prettier --write .",
    "prettier-check": "prettier --check .",
    "test": "vitest run"
  },
  "devDependencies": {
    "husky": "^5.0.9",
    "lint-staged": "^10.5.4",
    "prettier": "^3.9.6",
    "prettier-plugin-organize-imports": "^4.3.0",
    "typescript": "^5.9.3",
    "vue-tsc": "^2.2.12"
  },
  "lint-staged": {
    "*.{css,scss,graphql,html,json,js,jsx,md,mdx,ts,tsx,vue,yaml,yml}": "prettier --write"
  }
}
